a++
if (a <= 4)
{
    image_xscale += (a / 6)
    image_yscale += (a / 6)
}
if (instance_exists(bkyellowextra) && stay == 1)
{
    x = (bkyellowextra.xs + (bkyellowextra.ws / 2))
    y = (bkyellowextra.ys + (bkyellowextra.hs / 2))
}
 
